plotly>=5.0
Pillow==12.0.0
streamlit>=1.20.0
streamlit-plotly-events==0.0.6
rich>=10.14.0


# Optional (install if you plan to parse real EDF/WFDB/PDF files)
pyedflib>=0.1.29; platform_system != "Windows"   # pyedflib can be installed on Windows too, but may require wheels
wfdb>=3.4.0
pdf2image>=1.16.0
